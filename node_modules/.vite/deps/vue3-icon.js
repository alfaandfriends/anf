import "./chunk-L2QLVEU2.js";
import {
  computed,
  h
} from "./chunk-IKPHNDTQ.js";
import "./chunk-M6HDG4YY.js";
import "./chunk-DA74IYYC.js";

// node_modules/vue3-icon/dist/vue3-icon.mjs
var d = {
  transform: "rotate(var(--r, 0deg)) scale(var(--sx, 1), var(--sy, 1))"
};
var u = {
  fill: "currentColor"
};
var c = {
  mdi: {
    size: 24,
    viewbox: "0 0 24 24"
  },
  "simple-icons": {
    size: 24,
    viewbox: "0 0 24 24"
  },
  default: {
    size: 0,
    viewbox: "0 0 0 0"
  }
};
var g = {
  name: "icon",
  props: {
    type: {
      type: String,
      default: "mdi"
    },
    faIcon: {
      type: Object,
      default: null
    },
    path: {
      type: [String, Object, Array]
    },
    size: {
      type: [Number, String],
      default: 24
    },
    viewbox: String,
    flip: {
      type: String,
      validator: (e) => ["horizontal", "vertical", "both"].includes(e)
    },
    rotate: {
      type: [Number, String],
      default: 0
    }
  },
  setup(e) {
    if (!e.path && !e.faIcon)
      return console.warn("vue3-icon requires either a 'path' or an 'fa-icon' property"), () => h("div");
    const l = computed(() => {
      var t;
      return ((t = e.faIcon) == null ? void 0 : t.prefix) || e.type;
    }), i = computed(() => parseInt(e.rotate, 10)), r = computed(() => c[l.value] || c.default), o = computed(() => parseInt(e.size, 10) || r.value.size), s = computed(() => e.faIcon ? `0 0 ${e.faIcon.icon[0]} ${e.faIcon.icon[1]}` : false), f = computed(() => s.value || e.viewbox || r.value.viewbox), v = computed(() => ({
      ...d,
      "--sx": ["both", "horizontal"].includes(e.flip) ? "-1" : "1",
      "--sy": ["both", "vertical"].includes(e.flip) ? "-1" : "1",
      "--r": isNaN(i.value) ? i.value : i.value + "deg"
    })), h2 = computed(() => {
      var t;
      return e.faIcon ? (t = e.faIcon) == null ? void 0 : t.icon[4] : e.type === "simple-icons" && typeof e.path == "object" ? e.path.path : e.path;
    }), y = computed(() => l.value === "fad" ? (console.warn("vue3-icon does not currently support Duotone FontAwesome icons"), h("path")) : Array.isArray(e.path) ? h(
      "g",
      { style: { ...u } },
      e.path.map((t) => typeof t == "string" ? h("path", { d: t }) : h("path", { ...t }))
    ) : h("path", { d: h2.value, style: { ...u } }));
    return () => h(
      "svg",
      {
        style: v.value,
        class: ["vue3-icon"],
        width: o.value,
        height: o.value,
        viewBox: f.value
      },
      [y.value]
    );
  }
};
export {
  g as default
};
//# sourceMappingURL=vue3-icon.js.map
